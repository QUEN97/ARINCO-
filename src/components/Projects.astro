---
import ProyectoCard from "../components/ProyectoCard.astro";
import { getCollection } from "astro:content";

const proyectos = (await getCollection("proyectos")).slice(0, 3);
---

<section id="proyectos" class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <!-- Encabezado -->
      <div class="text-center mb-16">
        <span class="text-primary font-semibold tracking-widest uppercase"
          >Nuestro Trabajo</span
        >
        <h2 class="text-4xl font-bold text-dark mt-4 mb-6">
          Proyectos Destacados
        </h2>
        <div class="w-20 h-1 bg-primary mx-auto"></div>
      </div>

      <!-- Grid de proyectos -->
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {proyectos.map((proyecto: any) => <ProyectoCard proyecto={proyecto} />)}
      </div>

      <!-- Botón CTA -->
      <div class="text-center mt-12">
        <a
          href="/proyectos"
          class="inline-flex items-center bg-primary hover:bg-primary-dark text-white font-bold py-3 px-6 rounded-lg transition-colors duration-300 shadow-md hover:shadow-lg"
        >
          Ver todos los proyectos
          <svg
            class="w-5 h-5 ml-2 transition-transform hover:translate-x-1"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>